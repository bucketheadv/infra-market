# go-spring 框架启动时仍会加载本文件；与业务日志 internal/applog 无关。
appender:
  console:
    type: Console
    layout:
      type: TextLayout
      fileLineLength: 48
rootLogger:
  type: Root
  level: error
  appenderRef:
    ref: console
