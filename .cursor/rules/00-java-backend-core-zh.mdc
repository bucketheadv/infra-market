---
description: Java 后端通用协作规则（全局生效）
alwaysApply: true
---

# Java 后端通用规则

- 当前项目核心栈：Kotlin + Spring Boot + MyBatis-Flex + MySQL + Maven。
- 仅做与当前需求强相关的最小改动，不顺手重构无关模块。
- 优先复用仓库现有模式（分层、命名、工具类、异常体系），避免引入风格漂移。
- 未经明确要求，不新增三方依赖；能用 JDK/Spring/现有组件实现就不要扩库。
- 严禁硬编码密钥、Token、账号；配置统一走配置中心或环境变量。
- 涉及接口、消息、字段语义变化时，必须在回复中说明兼容性影响。
- 输出代码时默认中文说明，标识符遵循英文语义清晰、可维护。
- 除非用户明确要求，不主动新增测试文件或测试代码。

## 成熟实践基线

- 参考《阿里巴巴 Java 开发手册》的可读性与稳定性要求。
- 参考 Spring 官方建议：清晰分层、面向接口、避免控制层承载业务逻辑。
