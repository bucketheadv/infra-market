---
description: 配置管理与安全基线（Spring 配置文件优先）
globs: infra-market-server/src/main/resources/**/*.{yml,yaml,properties}
alwaysApply: false
---

# 配置与安全基线

- 环境隔离：本地、测试、生产配置分离，禁止把生产地址写入默认配置。
- 敏感配置（密码、密钥、Token）禁止明文入库；使用密文或外部密钥管理。
- 超时、重试、连接池大小必须显式配置，避免依赖隐式默认值。
- 新增开关类配置时，需给出默认值与回滚策略，便于灰度控制。
- 配置项命名保持业务语义一致，避免同义重复配置。
