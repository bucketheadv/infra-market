---
description: API 设计、异常处理与日志规范
globs: infra-market-server/src/main/kotlin/**/*.kt
alwaysApply: false
---

# API、异常与日志规范

- API 采用 RESTful 风格，统一响应结构，并使用 HTTP 状态码表达结果。
- 接口参数使用 `@Valid` 校验，分页查询默认支持分页参数。
- 路径命名使用小写与 `/` 分隔，禁止连字符 `-`、下划线 `_` 和驼峰路径。
- 对外接口返回统一错误语义，禁止向调用方透出底层堆栈信息。
- 自定义业务异常统一继承 `RuntimeException`，通过全局异常处理器集中处理。
- 禁止吞异常；要么明确降级，要么包装后抛出并附带业务上下文。
- 使用 SLF4J + Logback，关键业务操作记录日志，禁止记录敏感信息。
